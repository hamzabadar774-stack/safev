// Re-export the application shell layout from its component module.
// Keeping a dedicated `layouts/` entry point makes it clear which
// components define top-level page chrome vs. reusable widgets.
export { default } from "@/components/layout/AppLayout";